import {EDifficulty} from './enum/EDifficulty';

export class Game {
  // tslint:disable-next-line:variable-name
  private _positions: string[];
  // tslint:disable-next-line:variable-name
  private _name?: string;
  // tslint:disable-next-line:variable-name
  private _difficulty?: EDifficulty;
}
